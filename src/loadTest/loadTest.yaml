config:
  target: "http://localhost:5500"
  phases:
    - duration: 10
      arrivalRate: 100
  defaults:
    headers:
      Content-Type: "application/json"
  ensure:
    statusCode: 200

scenarios:
  - flow:
      - get:
          url: "/tickets"
